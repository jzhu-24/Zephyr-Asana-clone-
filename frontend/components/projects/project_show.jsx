import React from "react";
import { Link, withRouter } from "react-router-dom";

class ProjectShow extends React.Component {
  
  
  componentDidMount() {
    // this.props.requestColumns();
  }

  render() {
    // conditional blocking render

    // map over columns

    return (
      <div>
        
      </div>
    );
  }
}

export default withRouter(ProjectShow);
